<%- include('partials/header') %>
    <div class="table-container">
        <h2 class="todo-title">Todo list</h2>
        <div>
            <a href="/add-todo" class="btn">Add todo</a>
        </div>
        <br>
        <table>
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Created at</th>
                <th>Updated at</th>
                <th>Action</th>
            </tr>
            <tr>
                <!-- <% for(let i=0; i< todos.length; i++) { %>
                    <td>
                        <%= todos[i].title %>
                    </td>
                    <td>
                        <%= todos[i].desc %>
                    </td>
                    <td>
                        <%= todos[i].createdAt %>
                    </td>
                    <td>
                        <%= todos[i].updatedAt %>
                    </td>
                    <td>
                        <a href="/update-todo" class="btn">Edit</a>
                        <a href="/delete-todo" class="btn">Delete</a>
                    </td>
            </tr>
                <% } %> -->
                <!-- 
                <% for(todo of todos) { %>
                    <td>
                        <%= todo.title %>
                    </td>
                    <td>
                        <%= todo.desc %>
                    </td>
                    <td>
                        <%= todo.createdAt %>
                    </td>
                    <td>
                        <%= todo.updatedAt %>
                    </td>
                    <td>
                        <a href="/update-todo" class="btn">Edit</a>
                        <a href="/delete-todo" class="btn">Delete</a>
                    </td>
            </tr>
                <% } %> -->


                <% todos.forEach(todo=> { %>
            <tr>
                <td>
                    <%= todo.title %>
                </td>
                <td>
                    <%= todo.desc %>
                </td>
                <td>
                    <%= xyz(todo.createdAt).format("MM DD,YYYY,HH:MM:SS") %>
                </td>
                <td>
                    <%= xyz(todo.updatedAt).format("MM DD,YYYY,HH:MM:SS") %>
                </td>
                <td>
                    <a href="/update-todo?id=<%= todo._id %>" class="btn">Edit</a>
                    <a href="/delete-todo?id=<%= todo._id%>" class="btn">Delete</a>
                </td>
            </tr>
            <% }) %>

        </table>
    </div>
    <%- include('partials/footer') %>